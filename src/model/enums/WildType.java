package model.enums;

/**
 * Enum representing different types of wild cards in a card game.
 */
public enum WildType {
    /**
     * Represents a standard wild card.
     */
    WILD,

    /**
     * Represents a wild card that also forces the next player to draw four cards.
     */
    WILD_DRAW_4
}
